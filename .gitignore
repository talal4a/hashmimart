# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel
.env*.local

# Vercel OIDC token / secrets — never commit
production
.env
*.token

# Unrelated nested tool repo — not part of the app
after-effects-mcp

# Local Claude Code settings — machine-specific, contains auth tokens
.claude/settings.local.json
